# Claude Code

Read `AGENTS.md` first, then `docs/agent/INDEX.md` and the relevant files under `docs/agent/`.

Do not scan the whole repo before that. Do not violate `docs/agent/INVARIANTS.md`. Run `npm test` before and after code changes.
