{
  "name": "proxy-location-sync",
  "private": true,
  "version": "1.1.3",
  "type": "module",
  "description": "Chrome/Edge MV3 extension: detect the browser’s real proxy exit IP and sync webpage geolocation + timezone to that location.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xinian5216/proxy-location-sync.git"
  },
  "bugs": {
    "url": "https://github.com/xinian5216/proxy-location-sync/issues"
  },
  "homepage": "https://github.com/xinian5216/proxy-location-sync",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
